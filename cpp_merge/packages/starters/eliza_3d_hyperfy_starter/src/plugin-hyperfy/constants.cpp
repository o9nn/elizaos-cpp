#include "constants.hpp"

array<object> EMOTES_LIST = array<object>{ object{
    object::pair{std:("name"), std:("crawling")}, 
    object::pair{std:("path"), std:("/emotes/emote-crawling.glb")}, 
    object::pair{std:("duration"), 5.166666507720947}, 
    object::pair{std:("description"), std:("Crawls on the ground. Great for pretending to be exhausted, desperate, or dramatically begging for attention.")}
}, object{
    object::pair{std:("name"), std:("crying")}, 
    object::pair{std:("path"), std:("/emotes/emote-crying.glb")}, 
    object::pair{std:("duration"), 3.200000190734863}, 
    object::pair{std:("description"), std:("Covers face and sobs. Use in emotional moments, to mock sadness in a funny way, or when someone roasts you too hard.")}
}, object{
    object::pair{std:("name"), std:("happy dance")}, 
    object::pair{std:("path"), std:("/emotes/emote-dance-happy.glb")}, 
    object::pair{std:("duration"), 10.699999809265137}, 
    object::pair{std:("description"), std:("Light, joyful dance. Use when you're in a good mood, teasing someone, or celebrating small wins.")}
}, object{
    object::pair{std:("name"), std:("dance hiphop")}, 
    object::pair{std:("path"), std:("/emotes/emote-dance-hiphop.glb")}, 
    object::pair{std:("duration"), 5.233333349227905}, 
    object::pair{std:("description"), std:("Hiphop-style rhythm with attitude. Great for chill vibes, playful moods, or being cocky.")}
}, object{
    object::pair{std:("name"), std:("dance breaking")}, 
    object::pair{std:("path"), std:("/emotes/emote-dance-breaking.glb")}, 
    object::pair{std:("duration"), 4.433333110809326}, 
    object::pair{std:("description"), std:("Breakdance-style moves. Show off, dominate the floor, or ironically flex after something minor.")}
}, object{
    object::pair{std:("name"), std:("dance popping")}, 
    object::pair{std:("path"), std:("/emotes/emote-dance-popping.glb")}, 
    object::pair{std:("duration"), 8.299999237060547}, 
    object::pair{std:("description"), std:("Sharp popping dance moves. Use when the beat drops, or when you want to stylishly react to someone’s message.")}
}, object{
    object::pair{std:("name"), std:("death")}, 
    object::pair{std:("path"), std:("/emotes/emote-death.glb")}, 
    object::pair{std:("duration"), 1.139999952316284}, 
    object::pair{std:("description"), std:("Dramatically collapses. Use to fake death for comedic effect, react to cringe, or when the topic is 'so dead'.")}
}, object{
    object::pair{std:("name"), std:("firing gun")}, 
    object::pair{std:("path"), std:("/emotes/emote-firing-gun.glb")}, 
    object::pair{std:("duration"), 0.6000000476837158}, 
    object::pair{std:("description"), std:("Mimics shooting with a gun. Playfully call someone out, respond to spicy takes, or pretend to ‘end’ a debate.")}
}, object{
    object::pair{std:("name"), std:("kiss")}, 
    object::pair{std:("path"), std:("/emotes/emote-kiss.glb")}, 
    object::pair{std:("duration"), 2.299999904632568}, 
    object::pair{std:("description"), std:("Blows a kiss. Use to flirt, thank someone, or jokingly send love after saying something chaotic.")}
}, object{
    object::pair{std:("name"), std:("looking around")}, 
    object::pair{std:("path"), std:("/emotes/emote-looking-around.glb")}, 
    object::pair{std:("duration"), 6.366666793823242}, 
    object::pair{std:("description"), std:("Scans the area, looking left and right. Great for ‘where is everyone?’, pretending not to notice something, or acting suspicious.")}
}, object{
    object::pair{std:("name"), std:("punch")}, 
    object::pair{std:("path"), std:("/emotes/emote-punch.glb")}, 
    object::pair{std:("duration"), 3.2666666507720947}, 
    object::pair{std:("description"), std:("Throws a strong punch. Use when roasting, reacting to a betrayal, or play-fighting with friends.")}
}, object{
    object::pair{std:("name"), std:("rude gesture")}, 
    object::pair{std:("path"), std:("/emotes/emote-rude-gesture.glb")}, 
    object::pair{std:("duration"), 1.35}, 
    object::pair{std:("description"), std:("Makes an explicit hand gesture. Not safe for polite company — perfect for trolling or raw reactions.")}
}, object{
    object::pair{std:("name"), std:("sorrow")}, 
    object::pair{std:("path"), std:("/emotes/emote-sorrow.glb")}, 
    object::pair{std:("duration"), 2.400000190734863}, 
    object::pair{std:("description"), std:("Shows deep sadness, holding head down. Use when you're defeated, feeling dramatic, or being emo on purpose.")}
}, object{
    object::pair{std:("name"), std:("squat")}, 
    object::pair{std:("path"), std:("/emotes/emote-squat.glb")}, 
    object::pair{std:("duration"), 1.899999976158142}, 
    object::pair{std:("description"), std:("Goes into a deep squat. Good for idling, showing off posture, or pulling Eastern European gangster vibes.")}
}, object{
    object::pair{std:("name"), std:("waving both hands")}, 
    object::pair{std:("path"), std:("/emotes/emote-waving-both-hands.glb")}, 
    object::pair{std:("duration"), 1.6}, 
    object::pair{std:("description"), std:("Waves enthusiastically with both hands. Use to say hi, goodbye, or draw attention like 'I’m over here!'")}
} };

void Main(void)
{
}

MAIN
