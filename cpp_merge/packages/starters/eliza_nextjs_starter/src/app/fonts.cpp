#include "fonts.hpp"

object inter = object{
    object::pair{std:("className"), std:("font-sans")}, 
    object::pair{std:("style"), object{
        object::pair{std:("fontFamily"), std:("system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif")}
    }}
};

void Main(void)
{
}

MAIN
