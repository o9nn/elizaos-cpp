# GitHub token for API access (required)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# LLM Provider: 'anthropic' or 'openai' (default: anthropic)
PRR_LLM_PROVIDER=anthropic

# LLM Model for verification/analysis (default: claude-sonnet-4-5-20250929 for anthropic, gpt-5.2 for openai)
# Recommended example: claude-haiku-4-5-20251001 for fast, cost-effective verification
PRR_LLM_MODEL=claude-haiku-4-5-20251001

# Default fixer tool: cursor, opencode, claude-code, aider, codex, llm-api (default: cursor)
# WHY: Explicitly set this to lock in a specific tool when you have multiple
# installed, or to ensure consistent behavior across environments.
# RECOMMENDED: Leave commented out (auto-detect) for zero-config experience.
# PRR_TOOL=cursor

# Extended thinking token budget for Claude models (optional)
# PRR_THINKING_BUDGET=10000

# API Keys (at least one required depending on PRR_LLM_PROVIDER)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
