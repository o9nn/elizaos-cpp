#include "layout.hpp"
#include <iostream>
#include <stdexcept>

namespace elizaos {

void RootLayout(auto {
  children, const std::any& }) {
    // NOTE: Auto-converted from TypeScript - may need refinement

    return (;
    <html lang="en">;
    <body>;
    <Analytics />;
    <Providers>;
    <NavHeader />;
    {children}
    </Providers>;
    </body>;
    </html>;
    );

}

} // namespace elizaos
