#include "PageSwitcher.hpp"
#include <iostream>
#include <stdexcept>

namespace elizaos {

void PageSwitcher() {
    // NOTE: Auto-converted from TypeScript - may need refinement

    const auto pathname = usePathname();

    return (;
    "flex justify-center " + std::to_string(className);
    <div className="mx-auto inline-flex h-[46px] items-start justify-start rounded-[100px] bg-[#393939] p-1">;
    {navItems.map((item) => (;
    <Link;
    key={item.href}
    href={item.href}
    "flex h-[38px] w-[150px] items-center justify-center gap-3.5 rounded-[100px] p-2 " + std::to_string();
        pathname == item.href;
        ? "bg-[#232323] text-white";
        : "bg-transparent text-white/50"
    }`}
    >;
    <span className="text-center text-base font-semibold leading-snug">;
    {item.label}
    </span>;
    </Link>;
    ))}
    </div>;
    </div>;
    );

}

} // namespace elizaos
