#include "app.hpp"
#include <iostream>
#include <stdexcept>

namespace elizaos {

void App() {
    // NOTE: Auto-converted from TypeScript - may need refinement

    return (;
    <>;
    <GlobalStyles;
    styles={{
        ...scrollbarOverrides,
        ...audioOverrides,
    }}
    />;
    <ThemeProvider theme={theme}>;
    <DiscrubDialog />;
    </ThemeProvider>;
    </>;
    );

}

} // namespace elizaos
