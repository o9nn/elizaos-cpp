#include "/home/runner/work/elizaos-cpp/elizaos-cpp/eliza-3d-hyperfy-starter/src/plugin-hyperfy/constants.h"

array<object> EMOTES_LIST = array<object>{ object{
    object::pair{std::string("name"), std::string("crawling")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-crawling.glb")}, 
    object::pair{std::string("duration"), 5.166666507720947}, 
    object::pair{std::string("description"), std::string("Crawls on the ground. Great for pretending to be exhausted, desperate, or dramatically begging for attention.")}
}, object{
    object::pair{std::string("name"), std::string("crying")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-crying.glb")}, 
    object::pair{std::string("duration"), 3.200000190734863}, 
    object::pair{std::string("description"), std::string("Covers face and sobs. Use in emotional moments, to mock sadness in a funny way, or when someone roasts you too hard.")}
}, object{
    object::pair{std::string("name"), std::string("happy dance")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-dance-happy.glb")}, 
    object::pair{std::string("duration"), 10.699999809265137}, 
    object::pair{std::string("description"), std::string("Light, joyful dance. Use when you're in a good mood, teasing someone, or celebrating small wins.")}
}, object{
    object::pair{std::string("name"), std::string("dance hiphop")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-dance-hiphop.glb")}, 
    object::pair{std::string("duration"), 5.233333349227905}, 
    object::pair{std::string("description"), std::string("Hiphop-style rhythm with attitude. Great for chill vibes, playful moods, or being cocky.")}
}, object{
    object::pair{std::string("name"), std::string("dance breaking")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-dance-breaking.glb")}, 
    object::pair{std::string("duration"), 4.433333110809326}, 
    object::pair{std::string("description"), std::string("Breakdance-style moves. Show off, dominate the floor, or ironically flex after something minor.")}
}, object{
    object::pair{std::string("name"), std::string("dance popping")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-dance-popping.glb")}, 
    object::pair{std::string("duration"), 8.299999237060547}, 
    object::pair{std::string("description"), std::string("Sharp popping dance moves. Use when the beat drops, or when you want to stylishly react to someone’s message.")}
}, object{
    object::pair{std::string("name"), std::string("death")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-death.glb")}, 
    object::pair{std::string("duration"), 1.139999952316284}, 
    object::pair{std::string("description"), std::string("Dramatically collapses. Use to fake death for comedic effect, react to cringe, or when the topic is 'so dead'.")}
}, object{
    object::pair{std::string("name"), std::string("firing gun")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-firing-gun.glb")}, 
    object::pair{std::string("duration"), 0.6000000476837158}, 
    object::pair{std::string("description"), std::string("Mimics shooting with a gun. Playfully call someone out, respond to spicy takes, or pretend to ‘end’ a debate.")}
}, object{
    object::pair{std::string("name"), std::string("kiss")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-kiss.glb")}, 
    object::pair{std::string("duration"), 2.299999904632568}, 
    object::pair{std::string("description"), std::string("Blows a kiss. Use to flirt, thank someone, or jokingly send love after saying something chaotic.")}
}, object{
    object::pair{std::string("name"), std::string("looking around")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-looking-around.glb")}, 
    object::pair{std::string("duration"), 6.366666793823242}, 
    object::pair{std::string("description"), std::string("Scans the area, looking left and right. Great for ‘where is everyone?’, pretending not to notice something, or acting suspicious.")}
}, object{
    object::pair{std::string("name"), std::string("punch")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-punch.glb")}, 
    object::pair{std::string("duration"), 3.2666666507720947}, 
    object::pair{std::string("description"), std::string("Throws a strong punch. Use when roasting, reacting to a betrayal, or play-fighting with friends.")}
}, object{
    object::pair{std::string("name"), std::string("rude gesture")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-rude-gesture.glb")}, 
    object::pair{std::string("duration"), 1.35}, 
    object::pair{std::string("description"), std::string("Makes an explicit hand gesture. Not safe for polite company — perfect for trolling or raw reactions.")}
}, object{
    object::pair{std::string("name"), std::string("sorrow")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-sorrow.glb")}, 
    object::pair{std::string("duration"), 2.400000190734863}, 
    object::pair{std::string("description"), std::string("Shows deep sadness, holding head down. Use when you're defeated, feeling dramatic, or being emo on purpose.")}
}, object{
    object::pair{std::string("name"), std::string("squat")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-squat.glb")}, 
    object::pair{std::string("duration"), 1.899999976158142}, 
    object::pair{std::string("description"), std::string("Goes into a deep squat. Good for idling, showing off posture, or pulling Eastern European gangster vibes.")}
}, object{
    object::pair{std::string("name"), std::string("waving both hands")}, 
    object::pair{std::string("path"), std::string("/emotes/emote-waving-both-hands.glb")}, 
    object::pair{std::string("duration"), 1.6}, 
    object::pair{std::string("description"), std::string("Waves enthusiastically with both hands. Use to say hi, goodbye, or draw attention like 'I’m over here!'")}
} };

void Main(void)
{
}

MAIN
